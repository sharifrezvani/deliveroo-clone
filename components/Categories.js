import { Text, ScrollView } from "react-native";

const Categories = () => {
    return (
        <ScrollView>
            {/* CategoryCard */}
            <Text>Categories</Text>
        </ScrollView>
    )
}

export default Categories;